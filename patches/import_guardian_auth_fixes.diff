--- /dev/null
+++ b/lib/features/auth/domain/models/auth_user.dart
@@ -0,0 +1,74 @@
+// Auth User Domain Model
+// Clean Architecture - Domain Layer Entity
+
+class AuthUser {
+  const AuthUser({
+    required this.uid,
+    this.email,
+    this.displayName,
+    this.photoURL,
+    this.phoneNumber,
+    this.isEmailVerified = false,
+    this.isAnonymous = false,
+    this.createdTime,
+  });
+
+  final String uid;
+  final String? email;
+  final String? displayName;
+  final String? photoURL;
+  final String? phoneNumber;
+  final bool isEmailVerified;
+  final bool isAnonymous;
+  final DateTime? createdTime;
+
+  factory AuthUser.fromFirebaseUser(dynamic firebaseUser) {
+    return AuthUser(
+      uid: firebaseUser.uid,
+      email: firebaseUser.email,
+      displayName: firebaseUser.displayName,
+      photoURL: firebaseUser.photoURL,
+      phoneNumber: firebaseUser.phoneNumber,
+      isEmailVerified: firebaseUser.emailVerified ?? false,
+      isAnonymous: firebaseUser.isAnonymous ?? false,
+      createdTime: firebaseUser.metadata?.creationTime,
+    );
+  }
+
+  AuthUser copyWith({
+    String? uid,
+    String? email,
+    String? displayName,
+    String? photoURL,
+    String? phoneNumber,
+    bool? isEmailVerified,
+    bool? isAnonymous,
+    DateTime? createdTime,
+  }) {
+    return AuthUser(
+      uid: uid ?? this.uid,
+      email: email ?? this.email,
+      displayName: displayName ?? this.displayName,
+      photoURL: photoURL ?? this.photoURL,
+      phoneNumber: phoneNumber ?? this.phoneNumber,
+      isEmailVerified: isEmailVerified ?? this.isEmailVerified,
+      isAnonymous: isAnonymous ?? this.isAnonymous,
+      createdTime: createdTime ?? this.createdTime,
+    );
+  }
+
+  @override
+  bool operator ==(Object other) {
+    if (identical(this, other)) return true;
+    return other is AuthUser && other.uid == uid;
+  }
+
+  @override
+  int get hashCode => uid.hashCode;
+
+  @override
+  String toString() {
+    return 'AuthUser(uid: $uid, email: $email, displayName: $displayName)';
+  }
+}

--- /dev/null
+++ b/lib/features/auth/domain/repositories/i_auth_repository.dart
@@ -0,0 +1,57 @@
+// Auth Repository Interface
+// Clean Architecture - Domain Layer Interface
+
+import '../models/auth_user.dart';
+
+/// IAuthRepository
+/// 
+/// Domain layer interface that defines authentication operations.
+/// Presentation layer should only depend on this interface, not on 
+/// data layer implementations.
+abstract class IAuthRepository {
+  /// Get currently authenticated user
+  Future<AuthUser?> getCurrentUser();
+  
+  /// Sign in with email and password
+  Future<AuthUser?> signInWithEmailAndPassword(String email, String password);
+  
+  /// Create user with email and password  
+  Future<AuthUser?> createUserWithEmailAndPassword(String email, String password);
+  
+  /// Sign in with Google
+  Future<AuthUser?> signInWithGoogle();
+  
+  /// Sign in with Apple
+  Future<AuthUser?> signInWithApple();
+  
+  /// Sign in with phone number
+  Future<AuthUser?> signInWithPhoneNumber(String phoneNumber, String verificationCode);
+  
+  /// Sign out current user
+  Future<void> signOut();
+  
+  /// Send password reset email
+  Future<void> sendPasswordResetEmail(String email);
+  
+  /// Send email verification
+  Future<void> sendEmailVerification();
+  
+  /// Delete current user account
+  Future<void> deleteUser();
+  
+  /// Update user profile
+  Future<void> updateUserProfile({
+    String? displayName,
+    String? photoURL,
+  });
+  
+  /// Check if user is signed in
+  bool get isSignedIn;
+  
+  /// Stream of auth state changes
+  Stream<AuthUser?> get authStateChanges;
+}

--- a/lib/backend/backend.dart
+++ b/lib/backend/backend.dart
@@ -28,6 +28,7 @@ import '/features/notifications/domain/models/notifications_model.dart';
 import '/features/profile/domain/models/interest_model.dart';
 import '/features/voting/domain/models/weights_model.dart';
 import '/features/auth/domain/models/user_contents_model.dart';
+import '/features/auth/domain/models/premium_users_model.dart';
 import 'models/feed/poll_details_model.dart';
 import 'models/feed/feed_details_model.dart';
 import 'models/post/comments_model.dart';

--- a/lib/backend/legacy/backend_queries.dart
+++ b/lib/backend/legacy/backend_queries.dart
@@ -6,7 +6,7 @@
 import 'package:cloud_firestore/cloud_firestore.dart';
 
 // Import models temporarily until migration is complete
-import '../models/user/users_model.dart';
+import '../../features/profile/domain/models/user_profile.dart';
 import '../models/post/posts_model.dart';
 // CommentsModel is now in features
 import '../../features/posts/domain/models/comments_model.dart';
@@ -20,21 +20,21 @@ import '../../features/voting/domain/models/votes_model.dart';
 // ============================================================================
 
 /// 사용자 정보 조회 (단일 사용자)
-Future<UsersModel?> queryUserById(String userId) async {
+Future<UserProfile?> queryUserById(String userId) async {
   try {
     final doc = await FirebaseFirestore.instance
         .collection('users')
         .doc(userId)
         .get();
     
     if (doc.exists) {
-      return UsersModel.fromSnapshot(doc);
+      return UserProfile.fromSnapshot(doc);
     }
     return null;
   } catch (e) {
-    print('Error querying user by ID: $e');
+    print('Error querying UserProfile by ID: $e');
     return null;
   }
 }

 /// 여러 사용자 정보 조회
-Future<List<UsersModel>> queryUsersCollection({
+Future<List<UserProfile>> queryUsersCollection({
   Query Function(Query)? queryBuilder,
   int limit = -1,
 }) async {
@@ -51,7 +51,7 @@ Future<List<UsersModel>> queryUsersCollection({
     }
     
     final snapshot = await query.get();
-    return snapshot.docs.map((doc) => UsersModel.fromSnapshot(doc)).toList();
+    return snapshot.docs.map((doc) => UserProfile.fromSnapshot(doc)).toList();
   } catch (e) {
     print('Error querying users collection: $e');
     return [];
@@ -59,7 +59,7 @@ Future<List<UsersModel>> queryUsersCollection({
 }

 /// 사용자 검색 (displayName으로)
-Future<List<UsersModel>> searchUsersByDisplayName(String searchTerm) async {
+Future<List<UserProfile>> searchUsersByDisplayName(String searchTerm) async {
   return queryUsersCollection(
     queryBuilder: (query) => query
         .where('displayName', isGreaterThanOrEqualTo: searchTerm)

--- a/lib/backend/repositories/user_repository_legacy.dart
+++ /dev/null
@@ -1,191 +0,0 @@
-import 'package:get_it/get_it.dart';
-import '/backend/backend.dart';
-import '../models/migration/model_adapter.dart';
-import 'interfaces/i_user_repository.dart';
-import 'exceptions/repository_exception.dart';
-import 'exceptions/network_exception.dart';
-import 'exceptions/cache_exception.dart';
-import 'exceptions/validation_exception.dart';
-import 'mappers/user_mapper.dart';
-import '/services/cache/unified_cache_service.dart';
-
-/// User Repository 구현체
-/// 
-/// 사용자 데이터 관리를 위한 Repository 패턴 구현체입니다.
-/// 3-Layer 캐싱 전략을 통합하여 성능을 최적화합니다.
-class UserRepository implements IUserRepository {
-  
-  UserRepository({
-    FirebaseFirestore? firestore,
-    UnifiedCacheService? cacheService,
-  }) : _firestore = firestore ?? FirebaseFirestore.instance,
-       _cache = cacheService ?? GetIt.instance<UnifiedCacheService>();
-
-  final FirebaseFirestore _firestore;
-  final UnifiedCacheService _cache;
-  final UserMapper _mapper = UserMapper();
-
-  static const String _collectionPath = 'users';
-  
-  @override
-  Future<UserProfile?> getUserById(String userId) async {
-    try {
-      // Try cache first
-      final cacheKey = 'user_profile_$userId';
-      final cachedUser = await _cache.get<Map<String, dynamic>>(cacheKey);
-      
-      if (cachedUser != null) {
-        // Convert cached data using ModelAdapter
-        final userData = ModelAdapter.sanitizeUserData(cachedUser);
-        return UserProfile.fromMap(userData);
-      }
-      
-      // Fetch from Firestore
-      final doc = await _firestore.collection(_collectionPath).doc(userId).get();
-      
-      if (!doc.exists) {
-        return null;
-      }
-      
-      final userData = doc.data();
-      if (userData == null) {
-        return null;
-      }
-      
-      // Sanitize and adapt data
-      final sanitizedData = ModelAdapter.sanitizeUserData(userData);
-      final userProfile = UserProfile.fromMap(sanitizedData);
-      
-      // Cache for future use
-      await _cache.set(cacheKey, sanitizedData, ttl: Duration(minutes: 30));
-      
-      return userProfile;
-    } on FirebaseException {
-      throw NetworkException('Failed to fetch user from Firestore');
-    } catch (e) {
-      throw RepositoryException('Unexpected error while fetching user: $e');
-    }
-  }
-  
-  @override
-  Future<List<UserProfile>> getUsersByIds(List<String> userIds) async {
-    try {
-      final users = <UserProfile>[];
-      
-      // Process in batches of 10 (Firestore limitation)
-      const batchSize = 10;
-      for (int i = 0; i < userIds.length; i += batchSize) {
-        final batch = userIds.sublist(
-          i,
-          math.min(i + batchSize, userIds.length),
-        );
-        
-        // Try to get from cache first
-        final cachedUsers = <UserProfile>[];
-        final missingIds = <String>[];
-        
-        for (final userId in batch) {
-          final cacheKey = 'user_profile_$userId';
-          final cachedData = await _cache.get<Map<String, dynamic>>(cacheKey);
-          
-          if (cachedData != null) {
-            final sanitizedData = ModelAdapter.sanitizeUserData(cachedData);
-            cachedUsers.add(UserProfile.fromMap(sanitizedData));
-          } else {
-            missingIds.add(userId);
-          }
-        }
-        
-        users.addAll(cachedUsers);
-        
-        // Fetch missing users from Firestore
-        if (missingIds.isNotEmpty) {
-          final query = _firestore
-              .collection(_collectionPath)
-              .where(FieldPath.documentId, whereIn: missingIds);
-              
-          final snapshot = await query.get();
-          
-          for (final doc in snapshot.docs) {
-            final userData = doc.data();
-            final sanitizedData = ModelAdapter.sanitizeUserData(userData);
-            final userProfile = UserProfile.fromMap(sanitizedData);
-            
-            users.add(userProfile);
-            
-            // Cache the result
-            final cacheKey = 'user_profile_${doc.id}';
-            await _cache.set(cacheKey, sanitizedData, ttl: Duration(minutes: 30));
-          }
-        }
-      }
-      
-      return users;
-    } on FirebaseException {
-      throw NetworkException('Failed to fetch users from Firestore');
-    } catch (e) {
-      throw RepositoryException('Unexpected error while fetching users: $e');
-    }
-  }
-
-  @override
-  Future<void> updateUserProfile(String userId, UserProfile profile) async {
-    try {
-      // Validate profile data
-      if (profile.displayName?.isEmpty ?? true) {
-        throw ValidationException('Display name cannot be empty');
-      }
-      
-      if (profile.email?.isEmpty ?? true) {
-        throw ValidationException('Email cannot be empty');
-      }
-      
-      // Convert to Firestore-compatible format
-      final updateData = {
-        'displayName': profile.displayName,
-        'email': profile.email,
-        'photoUrl': profile.photoUrl,
-        'bio': profile.bio,
-        'birthDate': profile.birthDate?.millisecondsSinceEpoch,
-        'gender': profile.gender,
-        'location': profile.location,
-        'jobCategory': profile.jobCategory,
-        'jobName': profile.jobName,
-        'interests': profile.interests,
-        'hobbies': profile.hobbies,
-        'isPremium': profile.isPremium,
-        'updatedAt': FieldValue.serverTimestamp(),
-      };
-      
-      // Remove null values
-      updateData.removeWhere((key, value) => value == null);
-      
-      // Update in Firestore
-      await _firestore.collection(_collectionPath).doc(userId).update(updateData);
-      
-      // Invalidate cache
-      final cacheKey = 'user_profile_$userId';
-      await _cache.remove(cacheKey);
-      
-    } on FirebaseException catch (e) {
-      if (e.code == 'not-found') {
-        throw RepositoryException('User not found');
-      }
-      throw NetworkException('Failed to update user profile: ${e.message}');
-    } catch (e) {
-      if (e is ValidationException) rethrow;
-      throw RepositoryException('Unexpected error while updating user profile: $e');
-    }
-  }
-
-  @override
-  Future<void> deleteUser(String userId) async {
-    try {
-      await _firestore.collection(_collectionPath).doc(userId).delete();
-      
-      // Clear from cache
-      final cacheKey = 'user_profile_$userId';
-      await _cache.remove(cacheKey);
-      
-    } on FirebaseException {
-      throw NetworkException('Failed to delete user from Firestore');
-    } catch (e) {
-      throw RepositoryException('Unexpected error while deleting user: $e');
-    }
-  }
-}