===============================================
VOTING FEATURE ARCHITECTURE VIOLATIONS REPORT
===============================================
Generated: 2025-09-12
Scope: /lib/features/voting/
Mode: detect (detection only)
Total Files Scanned: 95+

EXECUTIVE SUMMARY
================
✅ GOOD: No presentation → data layer violations found
✅ GOOD: No cross-feature imports detected
❌ CRITICAL: Domain layer has Firebase dependencies (10 violations)
❌ HIGH: App layer imports feature data implementations (3 violations)
❌ MEDIUM: Legacy/Clean model mixing (Multiple violations)

VIOLATION CATEGORIES
==================

1. CRITICAL VIOLATIONS
======================

Firebase in Domain Layer (Domain → Infrastructure)
--------------------------------------------------
The domain layer should not depend on external infrastructure. These files violate Clean Architecture by importing Firebase directly:

VIOLATION: /domain/repositories/i_voting_repository.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Domain interface depends on concrete Firebase implementation
  FIX: Remove Firestore dependency, use abstract query builders

VIOLATION: /domain/usecases/stream_vote_counts_use_case.dart:2
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Use case tightly coupled to Firestore Query type
  FIX: Replace with domain-specific query abstractions

VIOLATION: /domain/usecases/get_vote_counts_use_case.dart:2
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Use case violates infrastructure independence
  FIX: Use repository pattern without exposing Firestore types

VIOLATION: /domain/usecases/stream_rankings_use_case.dart:2
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Domain logic depends on Firebase implementation
  FIX: Abstract away Firestore Query through repository

VIOLATION: /domain/coordinators/vote_state_coordinator.dart:3
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Coordinator should orchestrate domain services, not infrastructure
  FIX: Remove Firebase dependency, use domain services only

VIOLATION: /domain/models/weights_model.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Domain model depends on Firebase DocumentReference
  FIX: Use abstract ID/reference types

VIOLATION: /domain/models/votecounts_model.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Legacy model with Firebase coupling in domain
  FIX: Complete migration to clean VoteCounts model

VIOLATION: /domain/models/vote_expansion_requests_model.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Domain model violates infrastructure independence
  FIX: Replace DocumentReference with domain ID types

VIOLATION: /domain/models/rankings_model.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Domain model coupled to Firestore implementation
  FIX: Use clean domain model without Firebase dependencies

VIOLATION: /domain/models/votes_model.dart:1
  → import 'package:cloud_firestore/cloud_firestore.dart';
  IMPACT: Core domain model depends on external infrastructure
  FIX: Create clean Vote domain entity without Firebase coupling

2. HIGH VIOLATIONS
==================

App Layer → Feature Data Layer (Dependency Inversion)
----------------------------------------------------
The app layer should only depend on domain interfaces, not concrete data implementations:

VIOLATION: /app/di/notification_module.dart:23
  → import '../../features/voting/data/adapters/vote_service_impl.dart';
  IMPACT: App layer tightly coupled to concrete voting data implementation
  FIX: Remove direct import, register through domain interface only

VIOLATION: /app/di/voting_module.dart:4
  → import '../../features/voting/data/repositories/voting_repository_impl.dart';
  IMPACT: DI module exposes concrete repository implementation
  FIX: Register implementation but import only domain interfaces in app layer

VIOLATION: /app/di/voting_module.dart:6
  → import '../../features/voting/data/adapters/vote_service_impl.dart';
  IMPACT: App DI directly imports data layer implementation
  FIX: Use factory pattern with domain interfaces only

3. MEDIUM VIOLATIONS
====================

Legacy/Clean Model Mixing
--------------------------
The feature uses both VotecountsModel (legacy) and VoteCounts (clean) inconsistently:

INCONSISTENCY: Use case returns VotecountsModel but provider expects VoteCounts
  FILES: 
    - get_vote_counts_use_case.dart: Returns List<VotecountsModel>
    - voting_data_provider.dart: Converts to VoteCounts manually
  IMPACT: Manual conversion required, type safety lost
  FIX: Complete migration to VoteCounts domain model

INCONSISTENCY: Repository interface mixes models
  FILE: i_voting_repository.dart:76
  → Future<VotecountsModel?> getVoteCounts(String postId);
  IMPACT: Repository returns legacy model instead of domain model
  FIX: Return VoteCounts domain model consistently

INCONSISTENCY: Data provider manual conversion
  FILE: voting_data_provider.dart:153-160
  → Manual VotecountsModel to VoteCounts conversion
  IMPACT: Leaky abstraction, conversion logic in wrong layer
  FIX: Move conversion to repository/adapter layer

4. LOW VIOLATIONS
=================

Core Exports Import in Domain
-----------------------------
VIOLATION: /domain/models/votecounts_model.dart:8
  → import '/core_exports.dart';
  IMPACT: Domain model depends on framework exports
  FIX: Import only necessary dependencies explicitly

ARCHITECTURAL ANALYSIS
======================

Clean Architecture Compliance: 40/100
- ❌ Domain independence: Failed (Firebase dependencies)
- ❌ Dependency inversion: Partially violated (App → Data imports)  
- ✅ Feature isolation: Good (no cross-feature imports)
- ⚠️  Model consistency: Mixed (legacy/clean models coexist)

RECOMMENDED FIX PRIORITY
========================

Priority 1 (Critical - Immediate):
1. Remove all Firebase imports from domain layer
2. Create domain-specific query abstractions
3. Move Firebase coupling to data layer only

Priority 2 (High - This Sprint):
1. Fix app layer DI to use domain interfaces only
2. Complete VotecountsModel → VoteCounts migration
3. Create adapter layer for model conversion

Priority 3 (Medium - Next Sprint):
1. Clean up core_exports usage
2. Add architecture tests to prevent regressions
3. Document Clean Architecture guidelines

IMPACT ASSESSMENT
=================

Technical Debt: HIGH
- 10 critical violations blocking clean testing
- 3 high violations preventing proper DI
- Multiple inconsistent model usage

Testability Impact: SEVERE  
- Domain layer cannot be unit tested in isolation
- Firebase dependencies require complex mocking
- Repository tests need Firebase emulator

Migration Risk: MEDIUM
- VoteCounts model already exists (good foundation)  
- Gradual migration path available
- Breaking changes limited to internal APIs

AUTOMATED FIX AVAILABILITY
==========================

✅ Can auto-fix: App DI imports (use domain interfaces)
✅ Can auto-fix: VotecountsModel → VoteCounts conversion
❌ Cannot auto-fix: Firebase dependency removal (needs domain redesign)
❌ Cannot auto-fix: Query abstraction layer (architectural decision)

NEXT STEPS
==========

1. Run: --scope voting --mode fix --apply false  
   (Generate fix patches for app DI violations)

2. Manual: Design domain query abstractions  
   (Replace Firestore Query with domain interfaces)

3. Run: DIBinder for updated domain ports
   (Register new domain abstractions)

4. Execute: BuildSentinel for validation
   (Ensure fixes don't break functionality)

===============================================
END OF REPORT
===============================================