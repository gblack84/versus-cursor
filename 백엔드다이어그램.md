# Versus Space 백엔드 아키텍처 다이어그램

## 1. 전체 시스템 아키텍처 (활성화 상태 포함)

```mermaid
graph TB
    subgraph "클라이언트"
        A["✅ Flutter App<br/>(Active)"]
        B["📋 Web App<br/>(Planned)"]
    end
    
    subgraph "Firebase 생태계"
        C["✅ Firebase Auth<br/>(Active)"]
        D["✅ Firestore Database<br/>(Active)"]
        E["✅ Firebase Storage<br/>(Active)"]
        F["✅ Firebase Functions<br/>(Active - 4 functions)"]
        G["📋 Firebase Hosting<br/>(Planned)"]
    end
    
    subgraph "외부 AI 서비스"
        H["✅ Google Cloud Vision API<br/>(Active)"]
        I["✅ Perspective API<br/>(Active)"]
        J["✅ Gemini AI<br/>(Active)"]
        K["✅ Genkit Framework<br/>(Active)"]
    end
    
    subgraph "검색 엔진"
        L["✅ Algolia Search<br/>(Active)"]
    end
    
    A --> C
    A --> D
    A --> E
    A --> F
    B --> C
    B --> D
    B --> E
    B --> F
    
    F --> H
    F --> I
    F --> J
    F --> K
    
    D --> L
    
    style A fill:#e8f5e8
    style B fill:#e3f2fd
    style C fill:#e8f5e8
    style D fill:#e8f5e8
    style E fill:#e8f5e8
    style F fill:#e8f5e8
    style G fill:#e3f2fd
    style H fill:#e8f5e8
    style I fill:#e8f5e8
    style J fill:#e8f5e8
    style K fill:#e8f5e8
    style L fill:#e8f5e8
```

## 2. Firestore 데이터 모델 ERD

```mermaid
erDiagram
    USERS ||--o{ POSTS : creates
    USERS ||--o{ COMMENTS : writes
    USERS ||--o{ LIKES : gives
    USERS ||--o{ DISLIKES : gives
    USERS ||--o{ CHATS : participates
    USERS ||--o{ FRIENDS_LIST : has
    USERS ||--o{ CHARACTERS : owns
    USERS ||--o{ NOTIFICATIONS : receives
    
    POSTS ||--o{ COMMENTS : has
    POSTS ||--o{ LIKES : receives
    POSTS ||--o{ DISLIKES : receives
    POSTS ||--o{ RANKINGS : appears_in
    POSTS ||--o{ IMAGES : contains
    POSTS ||--o{ VIDEOS : contains
    
    POSTS {
        string id PK
        string question
        string title_a
        string title_b
        string description
        timestamp created_time
        string user_ref FK
        boolean is_anonymous
        list image_urls
        list video_urls
    }
    
    USERS {
        string uid PK
        string display_name
        string email
        string profile_picture
        int points_a
        int points_q
        timestamp created_time
        list interests
        list job_categories
    }
    
    COMMENTS {
        string id PK
        string content
        string post_ref FK
        string user_ref FK
        timestamp created_time
        boolean is_anonymous
    }
    
    LIKES {
        string id PK
        string post_ref FK
        string user_ref FK
        timestamp created_time
    }
    
    DISLIKES {
        string id PK
        string post_ref FK
        string user_ref FK
        timestamp created_time
    }
    
    CHATS {
        string id PK
        list participant_refs
        timestamp last_message_time
        string last_message
    }
    
    MESSAGES {
        string id PK
        string chat_ref FK
        string sender_ref FK
        string content
        timestamp sent_time
        boolean is_read
    }
    
    FRIENDS_LIST {
        string id PK
        string user_ref FK
        string friend_ref FK
        timestamp added_time
        string status
    }
    
    RANKINGS {
        string id PK
        string post_ref FK
        int rank_position
        int vote_count
        timestamp ranked_at
    }
    
    NOTIFICATIONS {
        string id PK
        string user_ref FK
        string type
        string title
        string content
        boolean is_read
        timestamp created_time
    }
    
    IMAGE_MODERATION {
        string id PK
        string image_url
        string user_id
        string session_id
        string box
        string moderation_status
        object safe_search_results
        timestamp moderated_at
    }
```

## 3. AI 검열 시스템 플로우

```mermaid
sequenceDiagram
    participant U as 사용자
    participant A as Flutter App
    participant FS as Firebase Storage
    participant CF as Cloud Functions
    participant V as Vision API
    participant P as Perspective API
    participant G as Gemini AI
    participant DB as Firestore
    
    U->>A: 이미지/텍스트 업로드
    A->>FS: 이미지 업로드 (메타데이터 포함)
    FS->>CF: Storage 트리거 발동
    
    par 이미지 검열
        CF->>V: SafeSearch 분석 요청
        V->>CF: 검열 결과 반환
    and 텍스트 검열
        CF->>P: 텍스트 독성 분석
        P->>CF: 독성 점수 반환
    end
    
    CF->>G: 종합 검증 요청 (Genkit)
    G->>CF: AI 검증 결과
    
    alt 부적절한 콘텐츠
        CF->>FS: 이미지 삭제
        CF->>DB: 검열 기록 저장
        CF->>A: 거부 응답
        A->>U: 검열 사유 표시
    else 적절한 콘텐츠
        CF->>DB: 승인 기록 저장
        CF->>A: 승인 응답
        A->>U: 업로드 완료 알림
    end
```

## 4. 콘텐츠 생성 및 발행 플로우

```mermaid
flowchart TD
    A[사용자 콘텐츠 작성] --> B{필수 필드 검증}
    B -->|실패| C[유효성 오류 표시]
    B -->|성공| D[이미지 업로드]
    
    D --> E[AI 검열 시작]
    E --> F{이미지 검열}
    F -->|거부| G[검열 사유 표시]
    F -->|승인| H{텍스트 검열}
    
    H -->|거부| I[수정 요청]
    H -->|경고| J{사용자 선택}
    H -->|승인| K[Gemini AI 검증]
    
    J -->|수정하기| I
    J -->|계속하기| K
    
    K --> L{AI 검증 결과}
    L -->|BLOCK| M[정책 위반 다이얼로그]
    L -->|WARNING| N[개선 제안 다이얼로그]
    L -->|PASS| O[타겟 오디언스 선택]
    
    N -->|수정하기| I
    N -->|계속하기| O
    
    O --> P[Firestore에 포스트 저장]
    P --> Q[Algolia 인덱싱]
    Q --> R[발행 완료]
    
    C --> A
    G --> A
    I --> A
    M --> A
    
    style A fill:#e3f2fd
    style E fill:#fff3e0
    style K fill:#f3e5f5
    style P fill:#e8f5e8
    style R fill:#e8f5e8
```

## 5. Firebase Functions 상세 기능 (배포 상태)

```mermaid
graph LR
    subgraph "Firebase Functions (7개 활성화)"
        A["✅ onUserDeleted<br/>(Active)"]
        B["✅ checkImageContent<br/>(Active)"]
        C["✅ moderateImage<br/>(Active)"]
        D["✅ validatePostContentWithGemini<br/>(Active)"]
        E["✅ onPostCreate<br/>(Active - 알림 시스템)"]
        N["✅ getUserPostingHistory<br/>(Active)"]
        O["✅ testNotificationSystem<br/>(Active - 테스트용)"]
    end
    
    subgraph "외부 API"
        F["✅ Perspective API<br/>(Active)"]
        G["✅ Vision API<br/>(Active)"]
        H["✅ Gemini AI<br/>(Active)"]
        I["✅ Genkit Framework<br/>(Active)"]
    end
    
    subgraph "Database"
        J["✅ Firestore<br/>(Active)"]
        K["✅ Firebase Storage<br/>(Active)"]
    end
    
    A --> J
    B --> G
    B --> J
    C --> G
    C --> K
    D --> H
    D --> I
    D --> F
    E --> I
    E --> J
    N --> J
    O --> J
    
    style A fill:#e8f5e8
    style B fill:#e8f5e8
    style C fill:#e8f5e8
    style D fill:#e8f5e8
    style E fill:#e8f5e8
    style N fill:#e8f5e8
    style O fill:#e8f5e8
    style F fill:#e8f5e8
    style G fill:#e8f5e8
    style H fill:#e8f5e8
    style I fill:#e8f5e8
    style J fill:#e8f5e8
    style K fill:#e8f5e8
```

## 6. AI 기반 알림 시스템 플로우

```mermaid
sequenceDiagram
    participant U as 사용자
    participant A as Flutter App
    participant CF as Cloud Functions
    participant AI as Genkit AI
    participant DB as Firestore
    participant N as NotificationService
    
    U->>A: 게시물 작성
    A->>A: 타겟 오디언스 선택<br/>(quick/public/custom/test)
    A->>DB: 게시물 저장
    DB->>CF: onCreate 트리거
    
    CF->>CF: 타겟 매칭 시작
    
    alt AI 추천 (quick)
        CF->>DB: 활성 사용자 조회
        DB->>CF: 후보 사용자 목록
        CF->>AI: 게시물-사용자 매칭 요청
        AI->>AI: 콘텐츠 분석<br/>사용자 프로필 분석<br/>관련성 점수 계산
        AI->>CF: 추천 사용자 목록
    else 전체 공개 (public)
        CF->>DB: 활성 사용자 랜덤 선택
        DB->>CF: 무작위 사용자 목록
    else 맞춤 설정 (custom)
        CF->>DB: 조건 기반 사용자 필터링
        DB->>CF: 필터링된 사용자 목록
    else 테스트 모드 (test)
        CF->>DB: 작성자 정보 조회
        DB->>CF: admin/tester 역할 확인
        CF->>CF: 작성자에게만 반복 전송
    end
    
    CF->>DB: 알림 배치 생성<br/>(notifications_record)
    CF->>DB: 게시물 통계 업데이트
    
    loop 각 타겟 사용자
        DB->>N: 실시간 리스너 트리거
        N->>A: 알림 표시
        A->>U: 투표 요청 알림
    end
    
    U->>A: 알림 상호작용<br/>(투표/나중에/닫기)
    A->>DB: 알림 상태 업데이트
```

## 7. 데이터베이스 컬렉션 구조

### 핵심 컬렉션 (35개)

#### 사용자 관리
- `users_record`: 사용자 프로필, 포인트, 설정
- `characters_record`: 사용자 아바타/캐릭터
- `settings_record`: 개인 설정
- `notifications_record`: 알림 관리

#### 콘텐츠 관리
- `posts_record`: A vs B 포스트 메인 데이터
- `comments_record`: 댓글 시스템
- `images_record`: 이미지 메타데이터
- `video_record`: 비디오 메타데이터
- `encodings_record`: 비디오 인코딩 상태

#### 소셜 기능
- `likes_record` / `dislikes_record`: 좋아요/싫어요
- `chats_record` / `group_chats_record`: 메시징
- `messages_record` / `group_messages_record`: 메시지 내용
- `friends_list_record`: 친구 관계

#### 게임화 요소
- `rankings_record`: 리더보드
- `point_record`: 포인트 거래 내역
- `votes_record` / `votecounts_record`: 투표 시스템
- `premium_users_record`: 프리미엄 구독

#### 검색 및 카테고리
- `searches_record`: 검색 기록
- `interest_record`: 관심사 카테고리
- `jops_category_record` / `jops_name_record`: 직업 분류

#### 콘텐츠 모더레이션
- `image_moderation_record`: 이미지 검열 결과
- `content_validations`: AI 검증 로그 (Functions에서 생성)

## 7. 보안 및 권한 관리

```mermaid
graph TD
    A[Firebase Auth] --> B{사용자 인증}
    B -->|익명| C[제한된 기능]
    B -->|인증됨| D[전체 기능]
    
    D --> E[Firestore Rules]
    E --> F{권한 검사}
    F -->|본인 데이터| G[읽기/쓰기 허용]
    F -->|공개 데이터| H[읽기만 허용]
    F -->|권한 없음| I[접근 거부]
    
    E --> J[Security Rules]
    J --> K[사용자별 데이터 격리]
    J --> L[공개/비공개 설정 적용]
    
    style A fill:#e8f5e8
    style E fill:#fff3e0
    style I fill:#ffcdd2
```

## 8. 성능 최적화 전략

### 데이터베이스 최적화
- **복합 인덱스**: 자주 쿼리되는 필드 조합
- **페이지네이션**: 무한 스크롤 구현
- **캐싱**: 자주 접근하는 데이터 로컬 캐싱

### 이미지 처리 최적화
- **다중 해상도**: original, display(800px), thumbnail(150px)
- **압축**: JPEG 85% 품질
- **CDN**: Firebase Storage 자동 CDN

### AI 서비스 최적화
- **토큰 사용량 추적**: Gemini AI 비용 관리
- **배치 처리**: 다중 이미지 병렬 검열
- **캐싱**: 검열 결과 재사용

## 9. 상태 표시 범례

### 🎯 상태 아이콘 의미
- **✅ Active**: 완전히 구현되고 현재 사용 중
- **🚧 Development**: 부분적으로 구현되거나 개발 중
- **❌ Inactive**: 구현되었지만 현재 비활성화
- **📋 Planned**: 계획되었지만 아직 구현되지 않음

### 🎨 색상 코드
- **녹색 (`#e8f5e8`)**: 활성화된 서비스
- **파란색 (`#e3f2fd`)**: 계획 중인 서비스
- **노란색 (`#fff3e0`)**: 개발 중인 서비스
- **회색 (`#f5f5f5`)**: 비활성화된 서비스

### 📊 현재 프로젝트 상태 요약
- **활성화된 서비스**: 11개 (Firebase 생태계 + AI 서비스)
- **계획 중인 서비스**: 2개 (Web App, Firebase Hosting)
- **배포된 Cloud Functions**: 7개 (알림 시스템 포함)
- **통합된 AI 서비스**: 4개 (Vision, Perspective, Gemini, Genkit)
- **사용 중인 Firestore 컬렉션**: 36개 (notifications_record 추가)
- **알림 시스템**: AI 기반 타겟팅 (4가지 모드)

### 🚀 최근 추가 기능 (2025-07-20)
- **AI 기반 알림 시스템**: Genkit을 활용한 스마트 사용자 매칭
- **타겟 오디언스**: quick(AI), public(랜덤), custom(조건), test(개발) 모드
- **실시간 알림**: NotificationService를 통한 즉시 알림
- **테스트 모드**: admin/tester 역할 전용 개발 도구

---

**작성일**: 2025-07-18  
**버전**: 1.2 (AI 알림 시스템 추가)  
**프로젝트**: Versus Space Flutter App  
**백엔드**: Firebase 생태계 + AI 서비스 통합  
**마지막 업데이트**: 2025-07-20